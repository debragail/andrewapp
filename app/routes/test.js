import Ember from 'ember';

let test = [{
	id: 'some-id',
	who: 'andrew wk',
	when: '2012',
	what 'lighting and photography'
}];
	
export default Ember.Route.extend({
	model() {
	    return item;
	}
});
